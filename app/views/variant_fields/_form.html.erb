<%= form_for(variant_field) do |f| %>
  <% if variant_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(variant_field.errors.count, "error") %> prohibited this variant_field from being saved:</h2>

      <ul>
      <% variant_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :belongs_to %>
    <%= f.text_field :belongs_to %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
